---
import Layout from '../../layouts/Layout.astro';
import PostDetails from '../../components/PostDetails';

const { id } = Astro.params;
---

<Layout title={`Post #${id}`} activePage="home">
  <PostDetails postId={Number(id)} client:load />
</Layout>
